# LEXGUARD Environment Configuration
PORT=8000
HOST=0.0.0.0

# LLM Keys (Mandatory for live API, fallback mode triggers if keys missing)
GROQ_API_KEY=your_groq_api_key_here
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Supabase (Optional: For persistent history & caching. If missing, local SQLite is used)
SUPABASE_URL=
SUPABASE_KEY=
